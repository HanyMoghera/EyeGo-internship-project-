# Copy to .env and adjust for your environment.
# Docker Compose API service sets these via compose file; local dev uses localhost values.

MONGODB_URI=mongodb://USER:PASSWORD@HOST:27017/DATABASE?authSource=admin
KAFKA_BROKER=HOST:9092
PORT=3000
